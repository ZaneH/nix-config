# M1 MacBook Air

This configuration is similar to my desktop. Still KDE.
The configuration expects a folder at =/etc/nixos/firmware=.

The SOPS key goes in =/var/lib/sops-nix/keys.txt=.

#+begin_src sh
sudo nixos-rebuild switch --flake .#mba --impure
#+end_src
